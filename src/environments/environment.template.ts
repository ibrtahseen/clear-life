// Copy this file to `environment.ts` and fill in your own Ummah API key.
// `environment.ts` is gitignored so real keys never get committed.
// Get a free key at https://ummahapi.com/register (raises the anonymous rate limit).
export const environment = {
  production: false,
  ummahApiKey: '',
};
